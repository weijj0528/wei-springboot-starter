<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.github.weijj0528</groupId>
        <artifactId>springboot-starter-example</artifactId>
        <version>${revision}</version>
    </parent>
    <artifactId>springboot-starter-example-redis</artifactId>
    <name>springboot-starter-example-redis</name>
    <url>https://github.com/weijj0528/wei-springboot-starter</url>
    <properties>
        <skip_maven_deploy>true</skip_maven_deploy>
        <maven.compiler.source>17</maven.compiler.source>
        <maven.compiler.target>17</maven.compiler.target>
        <flatten-maven-plugin.version>1.5.0</flatten-maven-plugin.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>
    <dependencies>
        <dependency>
            <groupId>com.github.weijj0528</groupId>
            <artifactId>wei-starter-redis</artifactId>
            <version>${parent.version}</version>
        </dependency>
        <!--Testcontainers 集成测试：真实 Redis 容器，替代外部 Redis 依赖-->
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <argLine>-Xmx1024m</argLine>
                    <!-- docker-java 默认 API 版本较低，部分 Docker 环境(如 OrbStack)要求 >=1.40，
                         显式协商 1.41 兼容；DOCKER_HOST 等连接信息交由 Testcontainers 自动探测 -->
                    <systemPropertyVariables>
                        <api.version>1.41</api.version>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
